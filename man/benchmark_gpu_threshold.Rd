% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarks.R
\name{benchmark_gpu_threshold}
\alias{benchmark_gpu_threshold}
\title{Benchmark GPU vs CPU Threshold for Common Operations}
\usage{
benchmark_gpu_threshold(
  sizes = c(1000, 10000, 1e+05, 1e+06, 1e+07),
  op = c("multiply", "add", "dot"),
  iterations = 10,
  verbose = TRUE
)
}
\arguments{
\item{sizes}{Numeric vector of input sizes to benchmark. Defaults to
powers of 10 from 1e3 to 1e7.}

\item{op}{Character string selecting the operation. One of
"multiply", "add", "dot". Defaults to "multiply".}

\item{iterations}{Number of timing iterations per size (default 10).}

\item{verbose}{Logical; print progress to console (default TRUE).}
}
\value{
A data.frame with columns: size, operation, cpu_time_ms,
  gpu_time_ms, speedup.
}
\description{
Measures execution time for CPU and GPU implementations of a chosen
operation across a range of input sizes, then produces a data frame that
can be plotted to visualise the crossover point where GPU becomes faster
than CPU (speedup > 1).
}
\examples{
\dontrun{
# Benchmark element-wise multiplication threshold
res <- benchmark_gpu_threshold(op = "multiply")
plot_gpu_threshold(res)
}
}
